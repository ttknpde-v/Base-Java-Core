package poins_one_one;

public class Parent {
        private String parentMessage = "I'm a Attribute in Class Parent";
        // create construct method
        // note ** when a class without construct method
        // program will crate construct default than it will set value
        public Parent () {
                System.out.println(parentMessage);
        }

}
